<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android" >

    <PreferenceCategory android:title="General">

        <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
        <!--<ListPreference
            android:defaultValue="-1"
            android:entries="@array/pref_example_list_titles"
            android:entryValues="@array/pref_example_list_values"
            android:key="example_list"
            android:negativeButtonText="@null"
            android:positiveButtonText="@null"
            android:title="@string/pref_title_add_friends_to_messages" />-->

        <ListPreference
            android:defaultValue="1"
            android:entries="@array/pref_adhan_sound_list"
            android:entryValues="@array/pref_adhan_sound_return_val"
            android:key="adhan_sound_pref"
            android:summary="Choose the style of Adhan"
            android:title="Set Adhan Sound"/>

        <ListPreference
            android:defaultValue="4"
            android:entries="@array/pref_quran_display_list"
            android:entryValues="@array/pref_quran_display_return_val"
            android:key="quran_display_pref"
            android:summary="Choose to display translations"
            android:title="Set Quran Display"/>
    </PreferenceCategory>

</PreferenceScreen>
